_parent_: configs/test.yaml

nerf:
    rand_rays_test: 4096                                         # number of random rays for each step20480
    render_video: true
    video_n_frames: 60
    video_rads_scale: 0.3

data_test:
    llff:
        test_views_method: fixed
